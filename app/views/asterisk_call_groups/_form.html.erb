<%= simple_form_for(@asterisk_call_group) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :group %>
    <%= f.input :members %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
