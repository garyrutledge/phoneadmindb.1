<%= simple_form_for(@asterisk_paging_group) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :paging_extension %>
    <%= f.input :name %>
    <%= f.input :members %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
